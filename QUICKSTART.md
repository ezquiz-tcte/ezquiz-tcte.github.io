# 快速開始指南

## 📋 部署步驟

### 1️⃣ 上傳到 GitHub

```bash
# 在專案資料夾中開啟命令提示字元
cd C:\Users\Administrator\Desktop\web

# 初始化 Git
git init

# 添加所有檔案
git add .

# 提交變更
git commit -m "Initial commit: EZQuiz 學習平台"

# 連接到你的 GitHub repository
git remote add origin https://github.com/你的使用者名稱/ezquiz.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 2️⃣ 啟用 GitHub Pages

1. 前往 GitHub repository 頁面
2. 點擊 **Settings**
3. 在左側選單點擊 **Pages**
4. 在 **Source** 區域選擇 `main` 分支
5. 點擊 **Save**
6. 等待幾分鐘，你的網站將在 `https://你的使用者名稱.github.io/ezquiz/` 上線

### 3️⃣ 設定 Decap CMS 後台

#### 選項 A：使用 Netlify（推薦）

1. 前往 [Netlify](https://www.netlify.com/) 並登入
2. 點擊 "New site from Git"
3. 選擇 GitHub 並授權
4. 選擇你的 repository
5. 保持預設設定，點擊 "Deploy site"
6. 部署完成後，進入 **Site settings** → **Identity**
7. 點擊 **Enable Identity**
8. 在 **Registration** 選擇 "Invite only"
9. 點擊 **Services** → **Git Gateway** → **Enable Git Gateway**
10. 回到 **Identity** 頁籤，點擊 "Invite users" 邀請自己
11. 收到郵件後設定密碼
12. 訪問 `你的netlify網址/admin/` 登入後台

#### 選項 B：本地開發測試

```bash
# 安裝 Decap 本地伺服器
npm install -g decap-server

# 在專案目錄執行
decap-server
```

在另一個終端機執行本地伺服器：
```bash
# 使用 Python
python -m http.server 8000

# 或使用 npx
npx http-server
```

訪問 `http://localhost:8000/admin/`

## 🎨 自訂內容

### 透過後台編輯（推薦）

1. 訪問 `/admin/`
2. 登入後即可編輯：
   - **首頁輪播圖**：新增/編輯 slider 圖片和文字
   - **團隊成員**：管理團隊成員資訊和照片
   - **網站設定**：修改聯絡資訊、社交媒體連結
   - **平台特色**：編輯特色功能說明

### 手動編輯 JSON 檔案

編輯以下檔案即可更新內容：
- `content/slider.json` - 首頁輪播圖
- `content/team.json` - 團隊成員
- `content/settings/site.json` - 網站基本資訊

## 🖼️ 更換圖片

### 方法 1：使用 Decap CMS 上傳
在後台直接上傳圖片，會自動儲存到 `images/uploads/` 資料夾

### 方法 2：手動上傳
1. 將圖片放到 `images/uploads/` 資料夾
2. 在 JSON 或 Markdown 檔案中引用圖片路徑：`/images/uploads/你的圖片.jpg`

### 方法 3：使用外部圖片
直接使用完整的 URL，例如：`https://example.com/image.jpg`

## 🔧 常見問題

### Q: 如何修改網站顏色？
A: 編輯 `css/style.css` 檔案中的 CSS 變數：
```css
:root {
    --primary-color: #4A90E2;  /* 改成你想要的顏色 */
}
```

### Q: 如何新增更多團隊成員？
A: 在 `/admin/` 後台的「團隊成員」區域點擊「新增」

### Q: 後台登入失敗？
A: 確認你已經：
1. 在 Netlify 啟用 Identity 和 Git Gateway
2. 透過郵件邀請並設定密碼
3. 使用正確的 Netlify 網站 URL

### Q: GitHub Pages 網站無法顯示？
A: 檢查：
1. GitHub Pages 是否已啟用
2. 分支是否選擇正確（main）
3. 等待約 5-10 分鐘讓 GitHub 建置網站

## 📞 需要協助？

查看完整的 README.md 文件獲取更多資訊。
